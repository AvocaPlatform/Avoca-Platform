{% extends 'layouts/manage.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">{{ recordVal(record, viewdefs.title) }}</h3>

                    <div class="box-tools pull-right">
                        <a class="btn btn-box-tool" href="{{ avoca_url(list_link) }}">
                            <i class="fa fa-list"></i>
                        </a>
                        <a class="btn btn-box-tool" href="{{ recordLink(edit_link, record) }}">
                            <i class="fa fa-pencil"></i>
                        </a>
                    </div>
                </div>
                <div class="box-body">
                    {% if recorddefs %}
                        <table class="table table-responsive-sm table-striped">
                            <tbody>
                                {% for group in recorddefs.fields %}
                                    <tr>
                                        {% for field, option in group %}
                                            <td>{{ fieldLabel(field, option) }}</td>
                                            <td>{{ recordVal(record, field, option) }}</td>
                                        {% endfor %}
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    {% else %}
                        <div class="alert alert-warning">{{ 'No config layout'|lang }}</div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}