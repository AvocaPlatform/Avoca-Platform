{% extends 'layouts/manage.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-header">
                    {{ recordVal(record, viewdefs.title) }}

                    <div class="card-header-actions">
                        <a class="card-header-action btn-setting" href="{{ avoca_manage(list_link) }}">
                            <i class="fa fa-list"></i>
                        </a>
                        <a class="card-header-action btn-setting" href="{{ recordLink(edit_link, record) }}">
                            <i class="fa fa-pencil"></i>
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    {% if recorddefs %}
                        <table class="table table-responsive-sm table-striped">
                            <tbody>
                                {% for group in recorddefs.fields %}
                                    <tr>
                                        {% for field, option in group %}
                                            <td>{{ fieldLabel(field, option) }}</td>
                                            <td>{{ recordVal(record, field, option) }}</td>
                                        {% endfor %}
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    {% else %}
                        <div class="alert alert-warning">{{ 'No config layout'|lang }}</div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}