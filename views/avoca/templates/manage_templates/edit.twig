{% extends 'templates/layouts/manage.twig' %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-header">
                    {{ __('Create') }}

                    <div class="card-header-actions">
                        <a class="card-header-action btn-setting" href="{{ avoca_admin('/') }}">
                            <i class="fa fa-list"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}